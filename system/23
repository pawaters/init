- With graphic session: login with username and password on GUI
- with ssh session: ssh username@127.0.0.1 after having ssh keys recogised by adding to authorized_keys of username the ssh key of where we are connecting from  (that IP being the localhost or loopback address. All computer use this address as their own, and that is how you connect using SSH)
